<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/icon?family=Material+Icons"
    />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"
    />
    <link rel="stylesheet" href="../css/cart.css" />
    <link rel="stylesheet" href="../css/wishlist.css" />
    <title>FRNTR</title>
  </head>
  <body>
    <%- include("./partials/nav.ejs")%>
    <% if(wishlist.item.length === 0) { %>
      <div class="signup1">
        <p class="sign1">Shopping cart</p>
      </div>
      <div class="emptyCartContainer">
        <i class="material-icons" id="emptyCart">production_quantity_limits</i>
        <div id="yourCart">YOUR CART IS EMPTY.</div>
        <div>
          Before proceed to checkout you must add some product to your shopping
          cart
        </div>
        <div>You will find a lot of interesting products on our "Shop" page.</div>
        <a href="/"><button id="returnButton">RETURN TO SHOP</button></a>
      </div>
    <% } else { %>
    <div class="fullWishlistContainer">
      <%wishlist.item.forEach((items) => { %>
      <div class="wishlistItemContainer">
        <div class="wishlistImageContainer">
          <img src="<%= products.find(products => products.id === items.productId).photo[0] %>" width="100px" height="100px" />
        </div>
        <div class="wishlistTexts">
          <a href="index" id="websiteLink">FRNTR</a>
          <div style="font-weight: bold"><%=items.productName%></div>
          <div>
            <s>16,100.00 EGP</s> <span style="color: red"><%=items.productPrice%></span>
            
          </div>
          <span>Stock:  <%=items.productQuantity%></span><button
          class="material-symbols-outlined" style="padding:0;"
          id="trash"
          onclick="deleteFromWishlist(`<%=items.productId%>`,`<%=items.productName%>` , `<%=items.productPrice%>`, `<%=items.quantity%>`)">
          delete</button>

          <div id="insideInfo">
            <p style="font-size: 10px">
              This bed is uniquie, the headboard is in glam deco style, with
              velvety panels rising in an arching pattern, giving it a new take
              on traitional channel tufting. Product...
            </p>
          </div>
        </div>
        <div class="buttonContainer">
          <button id="viewButton">
            <span id="viewMessage">View</span
            > <a href="/product/itempage?id=<%=items.productId %>&category=<%= products.find(products => products.id === items.productId).category %>">
              <span class="material-symbols-outlined" id="eyeIcon"
            >visibility</span
          ></a>
          </button>
          
        </div>
      </div>
      <%});%>
    </div>
    <% } %>
    <%- include("./partials/chat.ejs")%> <%- include("./partials/footer.ejs")%>
    <script type="text/javascript" src="../js/handler.js"></script>
    <script type="text/javascript"  src="/js/ajax-wishlist.js"></script>

  </body>
</html>
